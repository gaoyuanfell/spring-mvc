<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="moka.lineSend.dao.LineSendDao">

    <sql id="LineSend">
        lineId, userId, title, context, introduce, privacy, praised, review, forward, createDate
    </sql>

    <insert id="insert" parameterType="moka.lineSend.bo.LineSend" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO moka.linesend(
        <include refid="LineSend"/>
        ) VALUES (#{lineId}, #{userId}, #{title}, #{context}, #{introduce}, #{privacy}, #{praised}, #{review}, #{forward}, #{createDate});
    </insert>

    <resultMap id="map" type="moka.lineSend.to.LineSendTo">
        <id property="id" column="id"/>
        <association property="user" javaType="moka.user.to.UserTo" column="userId" select="moka.user.dao.UserDao.findOne"/>
        <association property="line" javaType="moka.lineSend.to.LineSendTo" column="lineId" select="moka.lineSend.dao.LineSendDao.findOne"/>
    </resultMap>

    <select id="findOne" parameterType="Integer" resultType="moka.lineSend.to.LineSendTo">
        SELECT * FROM moka.linesend WHERE id = #{id};
    </select>

    <select id="findOneOfUser" parameterType="moka.lineSend.vo.LineSendVo" resultType="moka.lineSend.to.LineSendTo">
        SELECT
        l.* ,
        (
        <!--<if test="userId != 0">-->
            SELECT COUNT(cr.id) FROM moka.commentrelation cr WHERE 1 = 1 AND cr.lineId = l.id AND cr.userId = #{userId} AND cr.comType = 1
        <!--</if>
        <if test="userId == 0">
            0
        </if>-->
        ) AS isPraised,
        (
        <!--<if test="userId != 0">-->
            SELECT COUNT(cr.id) FROM moka.commentrelation cr WHERE 1 = 1 AND cr.lineId = l.id AND cr.userId = #{userId} AND cr.comType = 3
        <!--</if>
        <if test="userId == 0">
            0
        </if>-->
        ) AS isForward
        FROM moka.linesend l WHERE id = #{id};
    </select>

    <select id="findOneAll" parameterType="Integer" resultMap="map">
        SELECT * FROM moka.linesend WHERE id = #{id};
    </select>
    
    <select id="findCount" resultType="Integer">
        SELECT COUNT(id) FROM moka.linesend;
    </select>

    <select id="findPage" parameterType="moka.basic.page.Page" resultMap="map">
        SELECT
        l.* ,
        (
        <if test="userId != 0">
            SELECT COUNT(cr.id) FROM moka.commentrelation cr WHERE 1 = 1 AND cr.lineId = l.id AND cr.userId = #{userId} AND cr.comType = 1
        </if>
        <if test="userId == 0">
            0
        </if>
        ) AS isPraised,
        (
        <if test="userId != 0">
            SELECT COUNT(cr.id) FROM moka.commentrelation cr WHERE 1 = 1 AND cr.lineId = l.id AND cr.userId = #{userId} AND cr.comType = 3
        </if>
        <if test="userId == 0">
            0
        </if>
        ) AS isForward
        FROM moka.linesend l LIMIT #{pageStart} , #{pageSize};
    </select>

    <update id="addPraised" parameterType="moka.lineSend.vo.LineSendVo">
        <if test="operationType">
            UPDATE moka.linesend SET line.praised = line.praised - 1 WHERE line.id = #{id};
        </if>
        <if test="!operationType">
            UPDATE moka.linesend SET line.praised = line.praised + 1 WHERE line.id = #{id};
        </if>
    </update>

    <update id="addReview" parameterType="moka.lineSend.vo.LineSendVo">
        <if test="operationType">
            UPDATE moka.linesend SET line.review = line.review - 1 WHERE line.id = #{id};
        </if>
        <if test="!operationType">
            UPDATE moka.linesend SET line.review = line.review + 1 WHERE line.id = #{id};
        </if>
    </update>

    <update id="addForward" parameterType="moka.lineSend.vo.LineSendVo">
        <if test="operationType">
            UPDATE moka.linesend SET line.forward = line.forward - 1 WHERE line.id = #{id};
        </if>
        <if test="!operationType">
            UPDATE moka.linesend SET line.forward = line.forward + 1 WHERE line.id = #{id};
        </if>
    </update>

</mapper>